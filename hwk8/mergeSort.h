#pragma once
#include <iostream>
#include <string>
#include "Students.h"
using namespace std;

class mergeSort
{
private:
	int middle, left, right, numItems;
public:
	studentList list;
	mergeSort()
	{

	}
	mergeSort()
	{
		numItems = numberOfItems;
		middle = numberOfItems / 2;
	}
};